<ul>
<table>
    <% @songs.each do |song| %>
    <div class="song">
      <tr><li><span><%= link_to song.title, song_path(song) %></span>
      <%= link_to "Delete", song, method: :delete, data: { confirm: "Really?" } %></li></tr>
    </div>
    <% end %>
</table>
</ul>